根据之前的讨论，你第一步的目标是**验证核心假设**：VisualPRM 在开放域（MM-Vet）上表现是否真的不如 LLaVA-Critic。

---

## 第一步详细任务说明（交给 Claude Code）

### 目标
在 MM-Vet benchmark 上跑 Best-of-8 评估，对比以下三个设置：
1. Pass@1（无 critic，baseline）
2. LLaVA-Critic-7B 作为 critic 做 BoN reranking
3. VisualPRM-8B 作为 critic 做 BoN reranking

**预期结论：VisualPRM 在开放域表现不佳，验证论文假设。**

---

### 环境要求
- Python 3.10+
- CUDA 环境，至少 2×80G GPU（需要同时跑生成模型 + critic 模型）
- 安装：`transformers`, `vllm`, `torch`, `Pillow`, `openai`（用于 MM-Vet 的 GPT-4 评分）

---

### Step 1：下载模型和数据

需要下载以下模型：
- 生成模型：`Qwen/Qwen2.5-VL-7B-Instruct`（用于生成候选回答）
- Critic 模型 A：`lmms-lab/llava-critic-7b`
- Critic 模型 B：`OpenGVLab/VisualPRM-8B`

需要下载 MM-Vet 数据集：
- 从 https://github.com/yuweihao/MM-Vet 下载，包含图片和问题的 JSON 文件

---

### Step 2：用 Qwen2.5-VL-7B 生成候选回答

对 MM-Vet 的每道题，用 Qwen2.5-VL-7B 生成 **8 个不同的回答**（temperature=0.8, top_p=0.9）。

输出格式存为 JSON：
```json
{
  "question_id": "v1_0",
  "image_path": "images/v1_0.png",
  "question": "...",
  "candidates": [
    "回答1", "回答2", "回答3", "回答4",
    "回答5", "回答6", "回答7", "回答8"
  ]
}
```

---

### Step 3：用两个 Critic 模型分别打分

**LLaVA-Critic 打分方式：**
- 输入：image + question + 单条候选回答
- 输出：pointwise 分数（1-10）
- 对 8 个候选分别打分，选分数最高的作为 BoN 结果

**VisualPRM 打分方式：**
- VisualPRM 是 step-level PRM，输入格式是多轮对话
- 但 MM-Vet 的回答通常不是 step-by-step 的推理链
- 处理方式：把每个候选回答按句子切分成若干"步骤"，喂给 VisualPRM 逐步打分，取所有步骤分数的均值或最小值作为该候选的总分
- 选总分最高的候选作为 BoN 结果

---

### Step 4：用 GPT-4o 对最终选出的回答评分

MM-Vet 的官方评估方式是用 GPT-4 对模型回答打分（0-1之间），需要：
- 调用 OpenAI API
- 按照 MM-Vet 官方评估脚本的 prompt 格式
- 对三组结果（Pass@1、LLaVA-Critic BoN、VisualPRM BoN）分别评估
- 输出每组的平均分

MM-Vet 官方评估脚本在其 GitHub repo 里有，直接使用即可。

---

### 最终输出

生成一个结果表格：

| 设置 | MM-Vet Score |
|---|---|
| Qwen2.5-VL-7B Pass@1 | xx.x |
| + LLaVA-Critic BoN@8 | xx.x |
| + VisualPRM BoN@8 | xx.x |

---

### 成功标准

如果 VisualPRM BoN@8 的分数**不高于甚至低于** Pass@1 或 LLaVA-Critic，说明假设成立，可以继续推进论文。

如果 VisualPRM 表现出乎意料地好，需要回来讨论论文方向是否需要调整。