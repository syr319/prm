# DistillPRM — Python 3.12, CUDA 12.6, Driver 560.35
# Install order matters: torch first, then flash-attn, then the rest.

# Core
torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0

# HuggingFace stack
transformers==4.57.1
accelerate==1.12.0
datasets==4.4.2
peft==0.18.0
tokenizers>=0.21

# Inference
vllm==0.18.1

# Attention kernel (pre-built wheel — match torch + CUDA version)
# flash-attn==2.8.3+cu128torch2.10
# Install separately:
#   pip install flash-attn --no-build-isolation

# Math / analysis
numpy==2.2.6
scipy==1.16.3
sympy==1.14.0
matplotlib==3.10.8

# Utilities
tqdm
sentencepiece
protobuf
